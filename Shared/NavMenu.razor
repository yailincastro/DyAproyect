<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">DyAproyect</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="login">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Login
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Celular">
                <span class="oi oi-plus" aria-hidden="true"></span> Catalogo de celulares
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Accesorio">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Catalogo De Accesorio/Cargadores
            </NavLink>
        </div>
         <div class="nav-item px-3">
            <NavLink class="nav-link" href="Cliente">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Registro de Clientes
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="facturacion">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Facturas
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Usuario">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Usuario Registrados
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="perfil">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Perfil
            </NavLink>
        </div>
         
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

      private async Task Logout()
    {
        var logout = await UserManagerService.Logout();
        if(logout.Success) 
            urlManager.NavigateTo("/login", true);
    }
}
